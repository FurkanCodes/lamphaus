# Android Design Requirements

The canonical Android interface and architecture requirements have moved to [`RULES.md`](../RULES.md).

This compatibility file is retained for existing links. It does not define separate requirements. If any older design note, implementation, or screenshot conflicts with `RULES.md`, follow `RULES.md`.

Exact TV token values continue to be implemented in `TvDesignTokens.kt` and `TvTheme.kt`; the repository's mobile/TV boundary and normative behavior are defined in `RULES.md`.
