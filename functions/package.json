{
  "name": "lamphaus-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "test": "npm run build"
  },
  "dependencies": {
    "@google-cloud/kms": "^6.0.0",
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
