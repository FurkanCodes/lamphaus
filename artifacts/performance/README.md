# Raw performance captures (git-ignored)

Store benchmark JSON, Perfetto traces, and profiler exports here. Each file is
referenced by path and SHA-256 from `docs/performance/results.md`.

Do not commit raw captures and never include provider URLs, credentials,
queries, tokens, stream locations, or account identifiers (`SHR-PROD-06`).
